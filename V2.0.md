# Portfolio V2 — Surgical Improvement Guide

### For Antigravity Dev Agent | Gourav Kumar Upadhyay

---

## CRITICAL ISSUES TO FIX (Priority Order)

### 1. Three.js / 3D Element Failures

### 2. Terminal Component Broken

### 3. Resume Content Inaccurate / Generic

### 4. Not Responsive (Mobile Broken)

### 5. Section-level Polish Issues

---

## ISSUE 1 — THREE.JS OVERHAUL

### Problem

The current Three.js implementation appears to be generic/broken — either not rendering correctly or using placeholder geometry. The hero 3D element and any scene components need a complete rethink.

### Replace With: Floating Particle Brain / Neural Constellation

**Implementation — Hero Background Scene:**

```tsx
// Hero3D.tsx — React Three Fiber
import { Canvas, useFrame, useThree } from '@react-three/fiber'
import { Points, PointMaterial, Float } from '@react-three/drei'
import * as random from 'maath/random'
import { useRef, Suspense } from 'react'

function StarField() {
  const ref = useRef<any>()
  const sphere = random.inSphere(new Float32Array(5000), { radius: 1.5 })

  useFrame((state, delta) => {
    if (ref.current) {
      ref.current.rotation.x -= delta / 12
      ref.current.rotation.y -= delta / 15
    }
  })

  return (
    <group rotation={[0, 0, Math.PI / 4]}>
      <Points ref={ref} positions={sphere} stride={3} frustumCulled={false}>
        <PointMaterial
          transparent
          color="#00D9FF"
          size={0.003}
          sizeAttenuation={true}
          depthWrite={false}
          opacity={0.6}
        />
      </Points>
    </group>
  )
}

// Neural Network Nodes — floating connected spheres
function NeuralNodes() {
  const meshRef = useRef<any>()
  
  useFrame(({ clock }) => {
    if (meshRef.current) {
      meshRef.current.rotation.y = clock.getElapsedTime() * 0.08
      meshRef.current.rotation.x = Math.sin(clock.getElapsedTime() * 0.05) * 0.15
    }
  })

  // 12 nodes at defined positions, connected by LineSegments
  const nodePositions = [
    [0, 0, 0], [1.2, 0.8, -0.5], [-1.1, 0.6, 0.3],
    [0.5, -1.0, 0.8], [-0.8, -0.9, -0.4], [1.5, -0.3, 0.2],
    [-0.3, 1.4, 0.6], [0.9, 0.3, -1.2], [-1.3, 0.1, -0.8],
    [0.2, -0.5, 1.3], [-0.6, -1.3, 0.7], [1.1, 1.1, 0.4],
  ]

  return (
    <group ref={meshRef}>
      {nodePositions.map((pos, i) => (
        <Float key={i} speed={1.5} rotationIntensity={0.3} floatIntensity={0.4}>
          <mesh position={pos as any}>
            <sphereGeometry args={[0.04 + (i % 3) * 0.015, 16, 16]} />
            <meshStandardMaterial
              color={i % 3 === 0 ? '#00D9FF' : i % 3 === 1 ? '#7B2FFF' : '#00FF9D'}
              emissive={i % 3 === 0 ? '#00D9FF' : i % 3 === 1 ? '#7B2FFF' : '#00FF9D'}
              emissiveIntensity={0.6}
              transparent
              opacity={0.85}
            />
          </mesh>
        </Float>
      ))}
    </group>
  )
}

export function Hero3DScene() {
  return (
    <Canvas
      camera={{ position: [0, 0, 2.8], fov: 60 }}
      style={{ position: 'absolute', inset: 0, pointerEvents: 'none' }}
      gl={{ antialias: true, alpha: true }}
    >
      <ambientLight intensity={0.3} />
      <pointLight position={[2, 2, 2]} intensity={1} color="#00D9FF" />
      <pointLight position={[-2, -2, -2]} intensity={0.5} color="#7B2FFF" />
      <Suspense fallback={null}>
        <StarField />
        <NeuralNodes />
      </Suspense>
    </Canvas>
  )
}
```

**Required packages:**

```bash
npm install @react-three/fiber @react-three/drei three maath
npm install --save-dev @types/three
```

**Mobile fallback — disable Three.js on screens < 768px:**

```tsx
const isMobile = useMediaQuery('(max-width: 768px)')
// On mobile: replace Canvas with a CSS animated gradient blob
```

---

### Alternative 3D: AI Circuit Board (Right side panel)

Instead of the hero background, place a** ****right-panel 3D object** — a glowing circuit board / torus knot that spins slowly:

```tsx
function CircuitTorus() {
  const ref = useRef<any>()
  useFrame(({ clock }) => {
    ref.current.rotation.x = clock.getElapsedTime() * 0.15
    ref.current.rotation.y = clock.getElapsedTime() * 0.25
  })
  return (
    <mesh ref={ref}>
      <torusKnotGeometry args={[1, 0.28, 200, 32, 2, 5]} />
      <meshStandardMaterial
        color="#00D9FF"
        emissive="#0066AA"
        emissiveIntensity={0.4}
        wireframe={true}
      />
    </mesh>
  )
}
```

---

## ISSUE 2 — TERMINAL COMPONENT FIX

### Problem

Terminal component either not animating, not rendering text correctly, or timing is off.

### Fix: Rebuild Terminal from Scratch

```tsx
// TerminalBoot.tsx
import { useState, useEffect } from 'react'

const BOOT_LINES = [
  { text: '> initializing gourav.dev...', delay: 0, suffix: ' [OK]', suffixColor: '#00FF9D' },
  { text: '> loading neural networks......', delay: 400, suffix: ' [OK]', suffixColor: '#00FF9D' },
  { text: '> mounting AI/ML systems.......', delay: 800, suffix: ' [OK]', suffixColor: '#00FF9D' },
  { text: '> compiling 3+ years experience', delay: 1200, suffix: ' [OK]', suffixColor: '#00FF9D' },
  { text: '> calibrating intelligence.....', delay: 1600, suffix: ' [OK]', suffixColor: '#00FF9D' },
  { text: '> ready.', delay: 2000, suffix: '', suffixColor: '' },
]

export function TerminalBoot({ onComplete }: { onComplete: () => void }) {
  const [visibleLines, setVisibleLines] = useState(0)
  const [done, setDone] = useState(false)

  useEffect(() => {
    BOOT_LINES.forEach((line, i) => {
      setTimeout(() => {
        setVisibleLines(i + 1)
        if (i === BOOT_LINES.length - 1) {
          setTimeout(() => { setDone(true); onComplete() }, 600)
        }
      }, line.delay)
    })
  }, [])

  return (
    <div
      style={{
        position: 'fixed', inset: 0, zIndex: 9999,
        background: '#020608',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        opacity: done ? 0 : 1,
        transition: 'opacity 0.5s ease',
        pointerEvents: done ? 'none' : 'all',
      }}
    >
      <div style={{
        fontFamily: 'JetBrains Mono, monospace',
        fontSize: 'clamp(12px, 1.5vw, 15px)',
        lineHeight: '2',
        color: '#8BA3B8',
        minWidth: '360px',
      }}>
        {BOOT_LINES.slice(0, visibleLines).map((line, i) => (
          <div key={i} style={{ display: 'flex', alignItems: 'center' }}>
            <span>{line.text}</span>
            {line.suffix && (
              <span style={{ color: line.suffixColor, fontWeight: 600, marginLeft: '4px' }}>
                {line.suffix}
              </span>
            )}
          </div>
        ))}
        {visibleLines < BOOT_LINES.length && (
          <div style={{ display: 'flex', alignItems: 'center' }}>
            <span>{BOOT_LINES[visibleLines]?.text.slice(0, 3)}</span>
            <span style={{
              display: 'inline-block', width: '8px', height: '16px',
              background: '#00D9FF', marginLeft: '2px',
              animation: 'blink 1s step-end infinite',
            }} />
          </div>
        )}
      </div>
      <style>{`@keyframes blink { 50% { opacity: 0; } }`}</style>
    </div>
  )
}
```

---

## ISSUE 3 — RESUME-ACCURATE CONTENT

> All placeholder content must be replaced with real data from the LaTeX resume. Name is** ****Gourav Kumar Upadhyay** (not "Gaurav").

### 3a. Hero Section — Correct Copy

```
// Small label (above H1):
"FULL STACK ENGINEER × AI/ML SYSTEMS"

// H1 — keep current "I build intelligent systems." — GOOD, keep it.

// Subtitle — replace with:
"3+ years shipping enterprise platforms across legal-tech, insurtech, 
travel-tech, and coworking. Now building the AI layer on top."

// Sub-subtitle / badge row:
[ India Accelerator · Gurugram ] [ Open to AI/ML Roles ] [ Remote OK ]

// CTA buttons:
Primary: "View Case Studies →"
Secondary: "AI Lab Demo"
Tertiary link: "Download Resume ↓"
```

### 3b. About Section — Real Story

```
// Section label: "MY INTRODUCTION / THE PIVOT"

// Headline:
"From Enterprise Platforms to Intelligent Systems"

// Body (replace ALL placeholder text):
"I've spent 3+ years architecting production systems across legal-tech, 
insurtech, and travel-tech — platforms processing 10K+ daily requests 
and serving real enterprise clients.

But something shifted in late 2024. I realized the most defensible products 
aren't just well-engineered — they're intelligent. So I went deep.

Not tutorial-deep. Production-deep.

I've since integrated OpenAI Vision API into vehicle damage detection 
pipelines, built RAG systems for document intelligence, and unified 5+ 
third-party APIs with Redis-cached gateways that cut response times from 
10 minutes to under 3 seconds.

I sit at the intersection of Full Stack Engineering and AI Systems — able 
to take an idea from model → API → production UI. That's where I'm building."

// Current role callout card:
"Currently: Full Stack Developer @ India Accelerator, Gurugram"
"Building: AI-powered vehicle claim platform (OpenAI Vision API)"

// 4 stat pills:
[ 3+ Years Production ] [ 7+ Live Products ] [ 10K+ Daily Requests ] [ 4 Domains ]
```

### 3c. Skills Section — Use Exact Resume Skills

Replace ALL placeholder skills with these exact groupings from the resume:

```
// TRACK A — Engineering Foundation:
Next.js / React.js     ████████████  92%
TypeScript / JS        ████████████  90%
NestJS / Node.js       ███████████   88%
PostgreSQL / Prisma    ██████████    82%
Redis / BullMQ         █████████     78%
Docker / Kubernetes    ████████      72%
AWS (EC2/S3/Lambda)    ████████      72%
GraphQL / WebSockets   ███████       68%

// TRACK B — AI/ML Arsenal:
OpenAI API / GPT-4     ████████████  88%
LangChain              ████████████  85%
RAG Pipelines          ███████████   82%
Vector DBs (Pinecone / ChromaDB)  ██████████  78%
Prompt Engineering     ██████████    80%
Hugging Face           ████████      70%
Python (AI/ML)         ████████      72%
OpenAI Vision API      █████████     75%

// ADDITIONAL BADGE ROW (below tracks):
Languages: JavaScript · TypeScript · Python · SQL · GraphQL · C++
Databases: PostgreSQL · MongoDB · MySQL · Redis · Firebase · Supabase · Pinecone
Cloud: AWS · GCP · Docker · Kubernetes · CI/CD · Nginx · TurboRepo
```

### 3d. AI Lab Section — Use Real Projects

Replace placeholder demo descriptions with real projects:

```
// Demo Card 1 — RAG Knowledge Base (LIVE):
Title: "RAG Knowledge Base"
Description: "Upload any PDF. Ask questions. Powered by LangChain, 
ChromaDB vector search, and OpenAI for grounded document Q&A."
Stack Pills: [Python] [LangChain] [ChromaDB] [OpenAI API] [Streamlit]
Live URL: https://rag-knowledge-basev2.streamlit.app/
Metric: "Semantic search across 100+ page documents in <2s"
Button: "Try Live Demo →" (links to the Streamlit app)

// Demo Card 2 — OpenAI Vision Pipeline:
Title: "AI Damage Detection Pipeline"  
Description: "Real-time vehicle image analysis using OpenAI Vision API. 
Detects damage, classifies severity, auto-fills claim fields."
Stack Pills: [OpenAI Vision] [NestJS] [Next.js] [PostgreSQL]
Metric: "↓40% manual claim assessment time"
Context: "Built at India Accelerator for enterprise insurance client"
Button: "View Architecture →" (opens architecture diagram modal)

// Demo Card 3 — Challan Gateway:
Title: "By Lawyered (Lawyered)"
Description: "Unified 5+ challan provider APIs with Redis caching. 
Reduced response time from 10+ minutes to <3 seconds."
Stack Pills: [Node.js] [Redis] [Prisma] [PostgreSQL] [REST]
Metric: "10K+ monthly requests · 99.9% uptime"
Live URL: https://lawyered.in/
```

### 3e. Projects / Case Studies — All 7 Real Projects

Replace generic project cards with these exact real projects:

---

**Project 1 — Paramount (AI Claim Platform)**

```
Tag: [AI/ML] [Enterprise]
Title: "Paramount — AI Vehicle Claim Platform"
Pitch: "Computer vision meets insurance workflows"
Problem: Manual vehicle damage assessment was slow, inconsistent, and costly.
Approach: Integrated OpenAI Vision API into real-time image analysis pipeline with multi-role approval workflows and insurance integrations.
Impact: ↓40% claim assessment time · Enterprise client production deployment
Stack: [Next.js] [NestJS] [PostgreSQL] [OpenAI Vision API] [Redis] [GCP]
Links: [Live ↗](https://sage.paramountservices.co.in/)
```

**Project 2 — Lawyered ChallanPay**

```
Tag: [Full Stack] [Legal-Tech]
Title: "ChallanPay — 5-Provider API Gateway"
Pitch: "10 minutes → 3 seconds. At scale."
Problem: Users had to check 5+ challan portals manually, each taking 2+ minutes.
Approach: Unified all providers into one Redis-cached gateway. Single API call returns unified result.
Impact: 10min → <3sec response · 10K+ monthly requests · 25% fewer deployment errors
Stack: [Node.js] [Prisma ORM] [PostgreSQL] [Redis] [AWS]
Links: [Live ↗](https://lawyered.in/)
```

**Project 3 — Bhraman Together**

```
Tag: [Full Stack] [Travel-Tech]
Title: "Bhraman Together — Travel & Booking Platform"
Pitch: "End-to-end travel platform with real-time geo intelligence"
Problem: Travelers needed one platform for booking, routing, and cab coordination.
Approach: Geospatial MongoDB queries + Google Maps route planning + real-time availability engine.
Impact: Sub-2s search results · Live production with real bookings
Stack: [NestJS] [Next.js] [MongoDB] [AWS RDS] [Google Maps API]
Links: [Live ↗](https://www.bhramantogether.com/)
```

**Project 4 — RAG Knowledge Base**

```
Tag: [AI/ML] [Personal Project]
Title: "RAG Knowledge Base — Document Intelligence"
Pitch: "Your documents, made conversational"
Problem: Long PDF documents are unsearchable and require full reads.
Approach: LangChain pipeline: PDF → chunking → ChromaDB embeddings → semantic retrieval → OpenAI answer generation.
Impact: Semantic search across 100-page docs in <2s · Live Streamlit demo
Stack: [Python] [LangChain] [OpenAI API] [ChromaDB] [Streamlit]
Links: [Live Demo ↗](https://rag-knowledge-basev2.streamlit.app/)
```

**Project 5 — IA Spaces Coworking**

```
Tag: [Full Stack] [Automation]
Title: "IA Spaces — Coworking Automation Platform"
Pitch: "From manual ops to automated workflows"
Problem: Coworking seat booking, WiFi access, and approvals were all manual.
Approach: Built automated WiFi provisioning, seat booking engine, and enterprise approval integrations.
Impact: Live at iaspaces.co · Multiple enterprise clients
Stack: [NestJS] [Next.js] [PostgreSQL] [TurboRepo]
Links: [Live ↗](https://iaspaces.co/)
```

**Project 6 — RukiyeZara**

```
Tag: [Full Stack] [Prop-Tech]
Title: "RukiyeZara — Property Booking Platform"
Pitch: "Sub-2s load times on a full-featured booking platform"
Problem: Property booking platforms are notoriously slow.
Approach: Next.js with optimized data fetching, Supabase backend, geolocation search.
Impact: <2s page load · Live production
Stack: [Next.js] [TypeScript] [Supabase]
Links: [Live ↗](https://www.rukiyezara.com/)
```

**Project 7 — Digital Vidya Saarthi**

```
Tag: [Full Stack] [Ed-Tech]
Title: "Digital Vidya Saarthi — School Management"
Pitch: "Full-stack school ops: attendance, enrollment, communication"
Stack: [React.js] [Node.js] [MongoDB] [Redux]
Links: [Live ↗](https://www.digitalvidyasaarthi.in/)
```

### 3f. Experience Section — Exact Resume Data

```
// Entry 1:
Role: "Full Stack Developer"
Company: "India Accelerator"
Location: "Sector 28, Gurugram"
Period: "Jul 2025 — Present"
Bullets:
• Architected enterprise platforms including vehicle claim management, legal-tech APIs, 
  and coworking automation using NestJS, Next.js, PostgreSQL, Redis, GCP, 
  TurboRepo monorepo, and AI integrations.
• Integrated OpenAI Vision API into real-time image verification pipelines for 
  AI-powered damage detection, reducing manual claim assessment time by ~40%.
• Built and deployed approval engines, operational automations, and WiFi access 
  provisioning systems, improving platform workflow efficiency across enterprise clients.
Impact number: "-40% claim time"

// Entry 2:
Role: "Full Stack Developer"
Company: "DoubleKlick Designs"
Location: "Okhla Phase-I, New Delhi"
Period: "Dec 2024 — Jul 2025"
Bullets:
• Developed scalable full-stack applications using Next.js, NestJS, Prisma ORM, 
  MongoDB, and AWS, delivering production-grade REST APIs with authentication flows.
• Unified 5+ challan provider APIs into a single optimized gateway using Redis caching, 
  slashing response time from 10+ minutes to under 3 seconds serving 10K+ monthly requests.
• Implemented CI/CD pipelines and AWS deployments (EC2, S3, Lambda), reducing 
  production deployment errors by 25%.
Impact number: "10min → 3sec"

// Entry 3:
Role: "Software Engineer"
Company: "Corplyx Technologies Pvt. Ltd."
Location: "Sector-62, Noida"
Period: "Jun 2023 — Nov 2024"
Bullets:
• Led scalable web application development, streamlining backend query performance 
  and frontend bundle sizes to achieve 30% reduction in page load times.
• Built React.js interfaces with Redux state management and Node.js/Express.js backends 
  with MongoDB for high-throughput data workflows.
Impact number: "-30% load time"

// Entry 4:
Role: "Software Developer"
Company: "Infotech Software Solutions"
Location: "Sector-10, Noida"
Period: "Sept 2022 — Jun 2023"
Bullets:
• Developed backend microservices, frontend components, and enhanced database 
  workflows resulting in 15% improvement in system response times.
Impact number: "+15% response"
```

### 3g. Resume Section — Correct Details

```
Name on resume: Gourav Kumar Upadhyay
Email: gaurav.upadhyay.vasudeva@gmail.com
Phone: +91 8920377548
Location: New Delhi, India
Portfolio: https://itsgauravhere.netlify.app/
GitHub: https://github.com/Poi5eN
LinkedIn: https://www.linkedin.com/in/gourav-kumar-upadhyay-0731b41b4

Education:
Greater Noida Institute of Technology
B.Tech, Computer Science | Jul 2018 — Jun 2022

Certifications (add to resume section as badges):
• Software Engineering — FreeCodeCamp
• Data Science / Intro to ML — Kaggle
• The AI Engineer Path — Scrimba
```

---

## ISSUE 4 — RESPONSIVENESS (Complete Spec)

### Breakpoints

```css
/* Define in globals.css or tailwind.config.ts */
xs:  320px  (small phones)
sm:  480px  (phones)
md:  768px  (tablets)
lg:  1024px (small laptops)
xl:  1280px (desktops)
2xl: 1536px (large screens)
```

### Section-by-Section Responsive Rules

#### Navigation

```
Desktop:  flex row — logo left, links center, CTA right
Tablet:   logo left, hamburger right (hide links)
Mobile:   logo left, hamburger right
Mobile menu: full-screen overlay, stacked links, stagger animation
```

#### Hero

```
Desktop:  two columns — text left (55%), 3D scene right (45%)
Tablet:   text full width, 3D scene behind (absolute, opacity 0.3)
Mobile:   text only, no Three.js (replace with CSS animated gradient)
          Font: H1 clamp(32px, 8vw, 72px)
```

#### About

```
Desktop:  two columns — story left, stats/image right
Tablet:   stack vertically, stats become 2x2 grid
Mobile:   full single column, stats 2x2 grid at reduced size
```

#### Skills (Dual Track)

```
Desktop:  two columns side by side
Tablet:   two columns (smaller)
Mobile:   single column, Track A then Track B stacked
```

#### AI Lab

```
Desktop:  two demo cards side by side
Tablet:   two cards (may be tighter)
Mobile:   single column stacked cards
```

#### Projects

```
Desktop:  3-column grid (or 2-column for featured + 3 small)
Tablet:   2-column grid
Mobile:   1-column stack, each card full width
          Hide "Problem/Approach" detail on mobile, show on expand
```

#### Experience Timeline

```
Desktop:  vertical timeline, content alternates left/right
Tablet:   timeline left-anchored, content always on right
Mobile:   no timeline line, just stacked cards with role/company/dates
```

#### Resume Section

```
All sizes:  centered, PDF viewer width 100% with max-width 800px
            Download button always visible, full width on mobile
```

#### Contact

```
Desktop:  centered, max-width 640px
Mobile:   full width, CTA buttons full width stacked
```

### Responsive Typography Scale

```css
/* Add to globals.css */
:root {
  --text-display: clamp(36px, 7vw, 80px);   /* H1 hero */
  --text-h2: clamp(24px, 4vw, 48px);         /* Section titles */
  --text-h3: clamp(18px, 2.5vw, 28px);       /* Card titles */
  --text-body: clamp(14px, 1.5vw, 16px);     /* Body text */
  --text-small: clamp(11px, 1.2vw, 13px);    /* Labels, pills */
}
```

### Touch / Mobile UX Fixes

```
- All tap targets minimum 44×44px
- No hover-only interactions (add touch equivalents)
- Project card tilt effect: DISABLED on touch devices
  (detect via: window.matchMedia('(hover: none)'))
- Custom cursor: DISABLED on touch devices
- Three.js Canvas: DISABLED on mobile, replaced with CSS animation
- Smooth scroll (Lenis): keep on mobile but reduce scroll multiplier
```

---

## ISSUE 5 — SECTION POLISH FIXES

### 5a. Hero — Specific Fixes

```
CURRENT ISSUE: "I build intelligent systems." text hierarchy OK but subtitle is generic.
FIX: Update subtitle to real content (see 3a above).

CURRENT ISSUE: Floating name "GAURAV" terminal effect looks disconnected.
FIX: Remove the standalone GAURAV terminal — it conflicts with the hero layout.
     The terminal should ONLY be the preloader boot sequence.
   
CURRENT ISSUE: Bottom scroll indicator missing or broken.
FIX: Add animated scroll indicator:
     <div class="scroll-indicator">
       <span>scroll</span>
       <div class="animated-line" /> // 2px line that grows then fades
     </div>
```

### 5b. About / Pivot Section

```
CURRENT ISSUE: "The Pivot to Intelligence" section has lorem-style placeholder copy.
FIX: Replace with real story from 3b above.

CURRENT ISSUE: The 4 skill category cards (Full Stack Systems, Cognitive Pipelines, 
               Autonomous Agents, LLMOps Infra) look disconnected.
FIX: Keep these but add real descriptions and icons:

Card 1 — Full Stack Systems:
  Icon: ti-layout-grid (or custom SVG)
  Desc: "Next.js · NestJS · PostgreSQL · Redis · AWS · GCP"
  
Card 2 — Cognitive Pipelines (AI):  
  Icon: ti-brain (or neural icon)
  Desc: "LangChain · RAG · Vector DBs · OpenAI · Embeddings"

Card 3 — Autonomous Agents:
  Icon: ti-robot
  Desc: "ReAct Agents · Tool Use · Multi-step Reasoning"

Card 4 — LLMOps Infra:
  Icon: ti-server
  Desc: "Prompt Engineering · Eval Pipelines · API Optimization"
```

### 5c. AI Lab Section

```
CURRENT ISSUE: AI Lab section has placeholder "Run with Agent" button 
               and no actual demo content.
FIX: 
  - Replace placeholder UI with the 3 real projects listed in 3d above
  - The "Run with AI Agent" CTA should link to the live Streamlit RAG demo
  - Add architecture flow diagram for the Vision API pipeline
  - Add a "Currently Building" live badge showing latest GitHub activity
  
CURRENT ISSUE: Bottom "demo cards" look empty/incomplete.
FIX: Implement full case study cards (see 3d above) with:
  - Real stack pills
  - Real metric callouts  
  - Real live links
  - Architecture diagram button (opens modal with simple SVG flow)
```

### 5d. Case Studies / Projects

```
CURRENT ISSUE: "FleetOptimizer AI" and "JobMatch LLM" are fictional names.
FIX: Replace with real project names from resume (see 3e above).
     Map real projects:
     - FleetOptimizer AI → "Paramount — AI Vehicle Claim Platform"
     - JobMatch LLM → "ChallanPay — API Gateway (Lawyered)"
     - Agentit Travel Plan → "Bhraman Together — Travel Platform"
     + Add: RAG Knowledge Base, IA Spaces, RukiyeZara

CURRENT ISSUE: Project screenshots are missing or broken.
FIX: Use real project screenshots. If unavailable, use a gradient placeholder 
     with the tech stack color scheme. Add screenshot via:
     - OG image from the live URL
     - Screenshot tool: use https://api.microlink.io?url={LIVE_URL}&screenshot=true
       for auto-screenshots of live URLs.
```

### 5e. Work History / Experience

```
CURRENT ISSUE: Content appears correct but timeline styling needs work.
FIX: 
  - Ensure each entry has the colored impact badge (see 3f above)
  - Add company logos or colored initials avatar
  - Timeline connector line should be gradient (cyan top → purple bottom)
  - "Currently Here" indicator on the top/most recent entry with pulsing dot
```

### 5f. Resume & Timeline Section

```
CURRENT ISSUE: Resume section appears to exist but may have layout issues.
FIX:
  - The PDF viewer should use react-pdf or an iframe embed
  - Ensure "Download Resume" button opens the actual PDF (host on /public/resume.pdf)
  - Add resume preview thumbnail that expands on click
  - Below the resume, add Certifications as 3 badge cards:
  
    [FreeCodeCamp] Software Engineering Certification [view →]
    [Kaggle] Intro to Machine Learning [view →]  
    [Scrimba] The AI Engineer Path [view →]
```

### 5g. Contact Section

```
CURRENT ISSUE: Contact section appears below "Writing & Research" — may be cut off.
FIX:
  - Availability status: "🟢 Available — Response within 24h"
  - Email: gaurav.upadhyay.vasudeva@gmail.com
  - Links: GitHub (Poi5eN) · LinkedIn · Portfolio
  - Add: preferred contact method note
  - Location: "New Delhi, India · Remote OK · Open to Relocate"
```

---

## NEW SECTION: "By the Numbers" Stats Bar

Add a horizontal stats ticker/bar between Hero and About:

```
[ 3+ Years ] [ 7 Live Products ] [ 10K+ Req/Day ] [ 4 Industries ] [ 40% Faster Claims ] [ 10min→3sec API ]
```

Implementation: Auto-scrolling marquee or static centered flex row with animated count-up on scroll entry.

---

## NEW SECTION: Writing & Research (Stub)

Keep this section but populate it with real stubs:

```
Post 1 (stub/WIP):
"Building a RAG Pipeline from Scratch: What the Tutorials Don't Tell You"
Tags: [AI/ML] [LangChain] [Production]
Status: [Coming Soon]

Post 2 (stub/WIP):
"How I Cut API Response Time from 10 Minutes to 3 Seconds with Redis"
Tags: [Backend] [Performance] [Redis]
Status: [Coming Soon]  

Post 3 (stub/WIP):
"OpenAI Vision API in Production: Lessons from an Insurance Platform"
Tags: [AI/ML] [OpenAI] [Enterprise]
Status: [Coming Soon]
```

---

## ANIMATION AUDIT — What to Fix/Keep/Add

### Keep (Working Well)

* GSAP scroll reveal animations (if implemented)
* Lenis smooth scroll (keep, ensure not conflicting with Three.js)
* Skill bar fill animations on scroll entry

### Fix These Animations

```
1. Preloader terminal: rebuild from scratch (see Issue 2)
2. Hero text stagger: ensure GSAP timeline fires AFTER preloader completes
3. Cursor: verify custom cursor works — test on desktop Chrome/Firefox/Safari
4. Number counter (stats): add intersection observer trigger, 
   use gsap.to({ val: 0 }, { val: TARGET, onUpdate }) pattern
```

### Add These Animations

```
1. Section number labels: slide in from left on first viewport entry
2. Project cards: 
   - stagger reveal (0.1s delay each card)
   - tilt.js hover (5° max, disabled on touch)
   - progress bar on card image (simulate "loading" the project)
3. Experience timeline:
   - line draws from top to bottom as you scroll (SVG stroke-dashoffset trick)
4. Skills radar:
   - bars fill from 0% → value over 800ms with easeOut
   - trigger only once per session
5. AI Lab cards:
   - subtle gradient border animation (rotating conic-gradient)
6. "Currently Building" widget:
   - Fetch latest commit from GitHub API:
     https://api.github.com/users/Poi5eN/events?per_page=5
   - Show last pushed repo + commit message
```

---

## PERFORMANCE CHECKLIST

```
☐ Next/Image for ALL project screenshots (width, height, priority on hero)
☐ Three.js Canvas: lazy loaded with dynamic import
   import dynamic from 'next/dynamic'
   const Hero3D = dynamic(() => import('./Hero3D'), { ssr: false })
☐ GSAP: import only what you use
   import { gsap } from 'gsap'
   import { ScrollTrigger } from 'gsap/ScrollTrigger'
☐ Fonts: use next/font (no external font CDN requests)
☐ Disable Three.js on mobile (saves ~200KB JS)
☐ Add loading="lazy" to all below-fold images
☐ Lighthouse target: Performance 85+, Accessibility 90+, SEO 95+
☐ Add meta tags: og:title, og:description, og:image, twitter:card
   og:title = "Gourav Kumar Upadhyay — Full Stack × AI/ML Engineer"
   og:description = "3+ years shipping enterprise AI systems. Currently at India Accelerator."
```

---

## QUICK WINS (Implement First — High Impact, Low Effort)

1. **Fix the name** — change "GAURAV" to "Gourav" everywhere
2. **Replace placeholder project names** with real project names + live links
3. **Add the live RAG demo link** to AI Lab section (it's already built!)
4. **Fix terminal** — copy the TerminalBoot component above verbatim
5. **Add the stats bar** between hero and about
6. **Add "Available" status badge** in hero and contact
7. **Add certifications** as badge cards in resume section
8. **Mobile nav hamburger** — ensure it works at all sizes
9. **Fix OG meta tags** — add real name, description, og:image
10. **GitHub activity widget** — single API call, big credibility boost

---

## FILE/COMPONENT CHECKLIST FOR DEV

```
src/
├── components/
│   ├── preloader/
│   │   └── TerminalBoot.tsx        ← REBUILD (see Issue 2)
│   ├── hero/
│   │   ├── HeroSection.tsx         ← UPDATE copy (see 3a)
│   │   └── Hero3DScene.tsx         ← REBUILD (see Issue 1)
│   ├── about/
│   │   └── AboutSection.tsx        ← UPDATE with real story (see 3b)
│   ├── skills/
│   │   └── DualTrackSkills.tsx     ← UPDATE with real skills (see 3c)
│   ├── ailab/
│   │   └── AILabSection.tsx        ← UPDATE with real demos (see 3d)
│   ├── projects/
│   │   └── CaseStudies.tsx         ← REPLACE with real projects (see 3e)
│   ├── experience/
│   │   └── WorkHistory.tsx         ← UPDATE with exact resume data (see 3f)
│   ├── resume/
│   │   └── ResumeSection.tsx       ← UPDATE with real links + certifications
│   └── contact/
│       └── ContactSection.tsx      ← UPDATE with real info (see 3g)
├── public/
│   └── resume.pdf                  ← ADD the actual PDF
└── app/
    └── layout.tsx                  ← ADD og: meta tags
```

---

*Version: v2.1 | Generated for antigravity dev agent* *All content sourced from: Gourav Kumar Upadhyay LaTeX resume + portfolio screenshot analysis*
